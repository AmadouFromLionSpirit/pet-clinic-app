package com.petclinicapp.repositories;

import com.petclinicapp.model.GeographicalOrigin;
import org.springframework.data.repository.CrudRepository;

public interface GeographicalOriginRepository extends CrudRepository<GeographicalOrigin, Long> {
}
